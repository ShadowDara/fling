print(45) 
print(true)
print(66, 420, 13)

fn add(x, y) {
	let result = x + y;
	print(result)

	return result
}

print(add(10, 10))

fn makeAdder (offset) {

  fn add (x, y)  {
    x + y + offset
  }

  add
}

const adder = makeAdder(1);

print(adder(10, 5))
